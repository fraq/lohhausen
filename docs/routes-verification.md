# Проверка URL и стартовых подписей

2026-09-06. RED: 4 fail / 1 pass, затем GREEN: 5/5 routes tests. Исходные 11 tests также проходят. SHA-256 tests/routes.test.js: `6c1a3e35b995b3270badbee3cdaa269383296eeb04c230b3f016e8c3c1f98a96`. Независимый read-only REFACTOR: переделок не требуется, направление зависимостей и границы раздачи соблюдены.

Chrome: все 7 разделов и 5 подразделений открыты прямой навигацией, проверены выделение меню, нужный отчет и заголовок вкладки. Все адреса отвечают HTTP200. `/unknown`, `/reports/nope`, EPUB и COORDINATION дают404. `/guide` открыта также в новой вкладке.

Переход guide → decisions, browser Back → guide, Forward → decisions и Reload → decisions прошел без изменения JSON сохранения. Элементы переходов — настоящие ссылки с href; обработчик не перехватывает modified/non-primary clicks.

Полный сценарий браузера прошел до120: URL стал `/debrief`. Подтвержденная новая партия возвращает `/`. Ни навигация, ни чтение инструкции не запускают расчет месяца.

Стартовая подпись: «Начало управления · впереди 120 месяцев». Отчет о жилье: «Данные на начало управления. Первый месяц еще не рассчитан». Сообщение: «Отчет „Жилье“ получен на начало управления». В заголовке, отчете, журнале и на оси графика используется осмысленное название начального снимка, числовой шаг для расчетов остается0.

Прокрутка: инструкция переведена из внешней CSS-grid в обычный поток документа. В Chrome при390×844 конец инструкции и подвал доступны: bottom подвала808,2px при высоте844px, PageUp и PageDown изменяют scrollY и возвращают к концу. Горизонтального переполнения нет. На широком экране нижние блоки также доступны. Исходное сообщение пользователя об остановке прокрутки не воспроизведено; запрос о его браузере, устройстве и последнем видимом заголовке остается без ответа. Поэтому специфичная для его среды причина не подтверждена.

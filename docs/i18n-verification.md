# Приемка переводов — 2026-09-06

COMPLETE. Спецификация: i18n-spec.md, 5 критериев. RED: 5 новых тестов падали (.tdd/i18n-red.txt). GREEN: 26/26 общего набора проходят. Синтаксис app/model/server/i18n проверен; исходные тесты не менялись.

| Критерий | Реализация | Проверка |
|---|---|---|
| AC-1: четыре языка | i18n.js, locales/*.js, app.js render | Unit-тест; Chrome: 7 разделов × 3 языка, без остаточного русского текста и подписей |
| AC-2: динамические значения | translate/interpolate; Intl.NumberFormat | Unit-тест месяца 4; Chrome: 5 отчетов × 3 языка, жилье +60, начало управления и английский итог месяца 120 |
| AC-3: URL и настройка | languageFrom; обработчик выбора языка | Unit-тест приоритета; JSON партии побайтно не меняется при смене языка |
| AC-4: адреса и история | localizedPath; navigationPath/popstate | Unit-тест query/hash; Chrome back/forward/reload, /reports?lang=de&foo=1#part |
| EC-1: fallback, записи, черновики | validLanguage; draft; translate=no | Unit-тест; Chrome: неизвестный язык в истории возвращает сохраненный de; налог 23 и личная запись сохраняются при de→fr |

Completeness: 5/5 критериев проверены тестами и браузером. Traceability: 5/5 имеют реализацию. Coherence: отдельная языковая настройка, книги и экономические уравнения при локализации не менялись, внешних зависимостей нет.

Независимый reviewer translate_de проверил интеграцию. Два WARNING устранены: сохранение дополнительных query/hash при навигации и fallback на сохраненный язык в popstate. Повторная браузерная проверка обоих случаев пройдена. Ledger: .tdd/findings.json — 0 CRITICAL, 2 WARNING (resolved), 0 SUGGESTION. Неразрешенных замечаний по переводу нет.

## Браузер

Chrome, отдельный контекст lohhausen-uat. Пользовательская партия не использовалась. На 390×844 все 21 сочетание раздела/языка имеют ширину документа 390px. Визуальный осмотр выявил сжатую кнопку хода; раскладка исправлена: высота 43px, ширина 222–239px на четырех языках. Французская инструкция достигает footer: нижняя граница 808px при viewport 844px. Исходная проблема прокрутки пользователя не воспроизведена, точная причина неизвестна.

Проверена новая игра с сохранением языка. Английский runFlowChecks: AC-1/AC-5/AC-3/AC-7 проходят, итог месяца 120. Поврежденный JSON на французском вызывает переведенное сообщение, блокирует ход и остается нетронутым до явного сброса. Личная запись «Мой прогноз: жильё +60. Ne pas traduire.» сохранена дословно после переключения языка.

В каждом словаре 476 фраз с проверенными числовыми параметрами; дополнительные динамические фразы — extra.js. Единицы фабрики: watches/month, Uhren/Mon., montres/mois. Википедия открывается отдельно с noopener noreferrer. Для французского интерфейса явно обозначена английская статья; источники: wiki-links.md.

## Сравнения отчетов

report-comparison-001 завершен: 5 новых тестов проходят, независимая проверка выполнена, 720 сценарных состояний пройдены на этапе модели. Снимки месяца 4 сравниваются с месяцем 3, даже если отчет тогда не запрашивался. База хранится с отчетом. Старые сохранения без базы читаются и показывают ее отсутствие. В UI вместо месяца 0 — начало управления. Общее число тестов после i18n: 26.

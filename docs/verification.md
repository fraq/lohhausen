# Приемка первой версии Лоххаузена

2026-09-06. STATUS: COMPLETE. Quality Gate: PASS.
Следующее действие: открыть локальную игру; при остановке сервера выполнить `npm start`.

Спецификация: 11 критериев, подтверждена пользователем. RED: 11 падающих проверок с импортируемым пустым API (`.tdd/red-output.txt`). GREEN: 11/11 проходят. Свежий независимый агент проверил реализацию: отдельный рефакторинг не потребовался; расчет, представление и хранение разделены, направление зависимостей соблюдено. Исправлен замеченный им пропуск числа работников в туристическом отчете, отображение проверено через DOM.

## Полнота и трассировка

Все 11 критериев проверены: 11 PASS, 0 FAIL, 0 MISSING. Для всех есть свидетельство в реализации: 11 MATCHED, 0 UNMATCHED.

| Критерий | Проверка `tests/model.test.js` | Реализация | Браузер |
|---|---|---|---|
| AC-1: старт 0 / 3700 / 120 | строка 16 | `src/model.js:32` createGame | PASS |
| AC-2: 3 месяца = 1+1+1 | 25 | `src/model.js:229` advance | модель |
| AC-3: жилье +60 через 12 месяцев | 35 | `src/model.js:64`, applyProjects | PASS, запуск в месяце 1, ввод в 13 |
| AC-4: продажа и склад | 46 | `src/model.js:79` trade | модель |
| AC-5: датированный отчет | 52 | `src/model.js:111`, `src/app.js:156` | PASS, 0 → ход 1 → обновление 1 |
| AC-6: восстановление всей партии | 62 | `src/model.js:253`, `src/model.js:285`, `src/app.js:23` | PASS, реальная перезагрузка в месяце 7 |
| AC-7: пять направлений разбора | 76 | `src/model.js:238`, `src/app.js:172` | PASS, месяц 120 |
| EC-1: остановка на 120 | 105 | `src/model.js:229` | кнопки хода отключены |
| EC-2: нулевое население | 113 | `src/model.js:206` | модель |
| EC-3: нехватка средств | 136 | `src/model.js:64` | цена и недоступность видны |
| ERR-1: поврежденное сохранение | 144 | `src/model.js:285`, `src/app.js:22` | PASS, исходная строка сохранена |

SHA-256 тестов совпадает с RED; во время GREEN тесты не переписывались:

```text
b05b16bba56f037ea224e182099c253e0de98a06ca51d4600912f6b59dfeb5a0  tests/model.test.js
5d31bc820a6bb7b9366dab0bc29876890ca118f19c782f12a258d653bae2ba87  tests/browser-checks.js
```

## Финальный запуск проверок

Проверено на Node.js v26.0.0 и реальном Chrome через Chrome DevTools.

```sh
npm test
npm run check
node --check src/visuals.js
node scripts/verify-scenarios.mjs
```

Все команды прошли. TypeScript, сборщик и линтер не настроены и для запуска не нужны; зависимости не устанавливались.

## Динамика модели

Шесть полных сценариев, 720 месячных состояний. На каждом шаге проверены конечность чисел, товарный баланс, изменение чистых средств ровно на месячный баланс, единая рабочая сила, неотрицательность величин, границы 0–100, ограничения туристов спросом и вместимостью, полнота истории и точное восстановление сериализованной партии. Допуск денежных/товарных равенств — 0,00001 для округления.

| Сценарий, месяц 120 | Казна | Долг | Выпуск | Благополучие |
|---|---:|---:|---:|---:|
| Без вмешательства | 0 | 5681,6 | 452,8 | 88,8 |
| Согласованные вложения | 2941,4 | 0 | 803,0 | 95,9 |
| Все политики на минимуме | 3042,6 | 0 | 262,6 | 34,4 |
| Все политики на максимуме | 0 | 42524,9 | 842,7 | 97,6 |
| Повышенный налог | 5073,4 | 0 | 803,0 | 92,7 |
| Реклама туризма без строительства | 0 | 16443,2 | 452,8 | 88,8 |

Согласованный план: налог 22, обслуживание 30, услуги 82, обучение 38, сбыт 42; жилье и модернизация запущены в месяце 0. Повышенный налог — тот же план со ставкой 35: население к концу 2916 против 3887. Отдельное сравнение туризма на шестом месяце: одинаковая реклама 60 дает 20 посетителей без проекта и 100 после ввода инфраструктуры. Параметры и точные сценарии записаны в `scripts/verify-scenarios.mjs`, результаты — `artifacts/scenario-results.json`.

Это проверка внутренней согласованности авторской модели. Она не доказывает исторической точности коэффициентов или эффективности психологического тренинга. В том числе высокая удовлетворенность может сочетаться с долгом; казна не заменяет остальные направления разбора.

## Проверки браузера

- `runFlowChecks()` прошел на широком экране и повторно при мобильной эмуляции 390×844: новая игра, отчеты, решения, жилье, ходы, завершение 120 и пять итоговых показателей.
- `preparePersistenceCheck()` → настоящая перезагрузка → `verifyPersistenceCheck()`: месяц 7, население 3657,122344, налог 23, исходная JSON-запись совпала точно. Повторено после последних правок интерфейса.
- `prepareCorruptSaveCheck()` → перезагрузка → `verifyCorruptSaveCheck()`: понятное сообщение, поврежденная строка `{"version":1,"policies":` сохранена. Новая партия доступна через подтверждаемый диалог.
- Все шесть разделов и заполненный отчет при 390 px: `documentElement.scrollWidth === documentElement.clientWidth === 390`. Новая игра доступна через мобильный подвал; диалог и сброс проверены.
- Район фабрики открывает отчет по Enter, SVG-районы имеют роль кнопки и подписи. Иконка сохранения имеет доступное имя. Графики подписаны и сопровождаются текстовыми числами. Карта также поддерживает пробел.
- Отчет туризма показывает число работников; уведомления размещены в потоке и не закрывают содержимое. Просмотрены снимки широкого обзора и мобильного отчета; графики, таблицы, формы и навигация читаемы.
- Ошибок и предупреждений в консоли приложения на заключительном прогоне нет. Намеренные HTTP-запросы к запрещенным путям выполнялись отдельно.

Браузерные проверки доступны из консоли приложения через `await import('/tests/browser-checks.js')`. Полный сценарий начинает работу с новой партии. Для проверки сохранения сохраните результат `preparePersistenceCheck()` в `sessionStorage`, перезагрузите страницу и передайте его `verifyPersistenceCheck()`.

## Соответствие ограничениям

Книги не изменялись и не включены в раздачу. HTTP вернул 200 для приложения и 404 для EPUB, `COORDINATION/state/codex.md`, исследовательской записки и пути за пределы приложения. Сервер слушает 127.0.0.1 и раздает явный список файлов. Исходники не запрашивают внешние API, шрифты или аналитику.

Русский интерфейс обозначает реконструкцию и собственные допущения; доступны раздел «О модели», источники и документы с коэффициентами. Финансы разложены на казну, долг и чистую позицию; жилье обозначает дефицит мест. Расчет отделен от DOM и localStorage. Публичной публикации, аккаунтов, мультиплеера и оценки интеллекта нет. Все применимые ограничения спецификации соблюдены, отклонений от шаблонов соседних файлов — 0. Историческая строка о согласовании инфраструктуры в спецификации относится к моменту ее утверждения; согласование завершено.

Findings, рассчитанные по `.tdd/findings.json`: **0 CRITICAL · 0 WARNING · 0 SUGGESTION**. Непогашенных замечаний приемки нет. Дополнительные проверки крайних политик и доступности подкрепляют обязательные критерии; новой функциональности вне согласованного объема не вводят.

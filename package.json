{
  "name": "lohhausen",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Educational reconstruction of Dietrich Dörner's Lohhausen city simulation ('The Logic of Failure')",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/model.js && node --check src/app.js && node --check scripts/serve.mjs"
  },
  "engines": { "node": ">=22" }
}
